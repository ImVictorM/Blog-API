const userServ = require('./userServ');
const categoryServ = require('./categoryServ');
const postServ = require('./postServ');
const loginServ = require('./loginServ');

module.exports = {
  userServ,
  categoryServ,
  postServ,
  loginServ,
};
